version: 1
frontend:
  phases:
    preBuild:
      commands:
        - npm ci
    build:
      commands:
        - npx ng build --configuration=static
  artifacts:
    baseDirectory: dist/valuation-frontend/browser
    files:
      - '**/*'
  cache:
    paths:
      - node_modules/**/*